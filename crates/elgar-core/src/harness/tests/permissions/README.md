# harness/tests/permissions

## Purpose

Tests for permission decisions around risky harness primitives.

These tests prove the policy layer is explicit before `bash`, `write`, or
`edit` execution is enabled.
