# Renderer

This folder renders core events as plain text.

It is not the TUI renderer. TUI layout, colors, scrolling, and panes live in
`elgar-tui`.

This renderer is useful for:

- simple CLI output
- debugging session contents
- tests that need plain event text
