#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

cargo install --path "$repo_root/crates/elgar-cli" --force

install_dir="${CARGO_HOME:-$HOME/.cargo}/bin"
printf 'installed elgar to %s/elgar\n' "$install_dir"
