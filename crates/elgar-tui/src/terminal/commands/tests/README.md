# terminal/commands/tests

## Purpose

Tests for terminal slash-command parsing and local command messages.

## Files

- `mod.rs` wires the command test modules.
- `parse_test.rs` tests how submitted text becomes a `TerminalCommand`.
- `messages_test.rs` tests user-facing help and command messages.
