# terminal/turn/tests

## Purpose

Tests for submitted prompt handling and provider-turn behavior.

## Future Files

- `provider_test.rs` should test provider turn timing and completion logic.
- `provider_worker_test.rs` should test worker completion and cancellation.
- `submitted_test.rs` should test submitted text routing.
