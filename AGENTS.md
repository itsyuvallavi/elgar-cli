# Elgar Agent Entry Point

Use the active agent instructions in:

```text
docs/agent/AGENTS.md
```

Key standing rules:

- Keep messages short and direct without dropping important details.
- After each implementation step, explain briefly what changed and why.
- Keep files small and responsibilities narrow.
- Add headers to source files.
- Use the stable roster in `docs/agent/AGENT_ROSTER.md`.
- Follow Linear as the execution map.
- Treat `docs/archive/` as historical reference only.
