# markdown

## Purpose

This folder contains tests for `markdown.rs`.

`markdown.rs` renders assistant markdown into terminal-friendly text.

## Files

- `tests/markdown_test.rs` tests plain text, code blocks, lists, tables, hidden raw details, and preformatted text.
